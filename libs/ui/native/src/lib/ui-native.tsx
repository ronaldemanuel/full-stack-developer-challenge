import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface UiNativeProps {}

export function UiNative(props: UiNativeProps) {
  return (
    <View>
      <Text>Welcome to ui-native!</Text>
    </View>
  );
}

export default UiNative;
